Needed:
pip install opencv_python
pip install Flask
pip install numpy
pip install pyzed
pip install flask_socketio
Also requires installation of ROS2 Humble on the host machine.


All needed CDNs are localized besides the GPS map visuals
